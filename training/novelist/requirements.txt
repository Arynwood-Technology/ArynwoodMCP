# Separate from the main app's requirements.txt on purpose - these are large
# (torch + CUDA deps) and only needed on the machine actually running training,
# same reasoning as the dedicated whisper-venv / sycamore .venv elsewhere in this repo.
torch
transformers
peft
bitsandbytes
trl
accelerate
datasets
httpx
