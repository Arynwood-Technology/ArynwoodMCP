[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    eval: slow tests against live Ollama/Qdrant asserting on real model behavior (roadmap 3.3) — excluded by default, run explicitly with `pytest -m eval`
addopts = -m "not eval"
